#include <bits/stdc++.h>
using namespace std;

int main()
{
    cin.tie(nullptr)->sync_with_stdio(false);
    char ch;
    cin >> ch;
    cout << "  " << ch << '\n';
    cout << " " << ch << ch << ch << '\n';
    cout << ch << ch << ch << ch << ch;
    return 0;
}
