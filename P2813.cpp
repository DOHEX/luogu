//FIXME
#include <bits/stdc++.h>
using namespace std;

int n, m, a[100005], b[100005];

int main()
{
    cin.tie(nullptr)->sync_with_stdio(false);
    cin >> n >> m;
    for (int i = 1;i <= n;++i) {
        cin >> a[i];
    }
    for (int i = 1;i <= m;++i) {
        cin >> b[i];
    }
    sort(a + 1, a + n + 1);
    sort(b + 1, b + m + 1);
    int idx = 1;idx2 = 1;
    for
        for
            return 0;
}
