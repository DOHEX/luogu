#include <bits/stdc++.h>
using namespace std;

unsigned short a, b, c;

int main()
{
    cin.tie(nullptr)->sync_with_stdio(false);
    cin >> a >> b >> c;
    cout << a * 0.2 + b * 0.3 + c * 0.5;
    return 0;
}
