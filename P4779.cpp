#include <bits/stdc++.h>
using namespace std;

int n,m,s;

int main() {
    cin.tie(nullptr)->sync_with_stdio(false);
    cin>>n>>m>>s;
    int u,v,w;
    for(int i=1;i<=m;i++){
        cin>>u>>v>>w;
    }
    return 0;
}
