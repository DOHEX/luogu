#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(nullptr)->sync_with_stdio(false);
    int x;
    cin >> x;
    cout << "Today, I ate " << x << " apple" << (x > 1 ? "s." : ".");
    return 0;
}
